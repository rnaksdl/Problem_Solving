#include <stdio.h>

int main() {
        int a;
        printf("Enter a number:");
        scanf("%d", &a);

        if(a ==10) {
                printf("Condition is true.\n");
        }
        else{
                printf("Condition is false.\n");
        }

        return 0;
}